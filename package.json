{
  "name": "custom-footer-react",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "shopify": "shopify",
    "build": "shopify app build",
    "dev": "shopify app dev",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy"
  },
  "dependencies": {
    "@shopify/app": "^3.15.0",
    "@shopify/cli": "^3.64.1",
    "moment-timezone": "^0.5.45"
  },
  "trustedDependencies": [
    "@shopify/plugin-cloudflare"
  ],
  "author": "gregcraft",
  "private": true,
  "workspaces": [
    "extensions/*"
  ]
}
